# Migration `20191102174005-add-hp-slug-and-review-takeaways`

This migration has been generated by lvnr at 11/2/2019, 5:40:05 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191031014239-extend-headphone-review-criteria..20191102174005-add-hp-slug-and-review-takeaways
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://audiophile:LevAr8523*@audiophile-tools.clqkmkirkmxi.us-east-1.rds.amazonaws.com:5432/prisma"
 }
@@ -28,8 +28,9 @@
 model Headphone {
   id          String    @default(cuid()) @id
   createdAt   DateTime  @default(now())
   updatedAt   DateTime  @updatedAt
+  slug        String    
   model       String    @unique
   company     Company
   url         String?
   amazonUrl   String?
@@ -76,8 +77,11 @@
   credibility Int       @default(5)
   url         String?
   reviewer    String?
+  video       Boolean?
+  notes       String?
+  takeaways   String[]
   priceVsPerf       Int?
   priceVsPerfUrl    String?
   aesthetics        Int?
```

## Photon Usage

You can use a specific Photon built for this migration (20191102174005-add-hp-slug-and-review-takeaways)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191102174005-add-hp-slug-and-review-takeaways'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
