# Migration `20191103155958-add-bass-and-fun-to-reviews`

This migration has been generated by lvnr at 11/3/2019, 3:59:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."HeadphoneReview" ADD COLUMN "bass" integer   ,
ADD COLUMN "fun" integer   ,
ADD COLUMN "funUrl" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191103153702-typo-fix..20191103155958-add-bass-and-fun-to-reviews
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://audiophile:LevAr8523*@audiophile-tools.clqkmkirkmxi.us-east-1.rds.amazonaws.com:5432/prisma"
 }
@@ -97,14 +97,17 @@
   imaging           Int?
   imagingUrl        String?
   separation        Int?
   separationUrl     String?
+  fun               Int?
+  funUrl            String?
+  bass              Int?
+  bassUrl           String?
   bassQty           Int?
   bassClarity       Int?
   bassExtension     Int?
   bassTightness     Int?
   bassImpact        Int?
-  bassUrl           String?
   midrange          Int?
   midrangeUrl       String?
   treble            Int?
   trebleExtension   Int?
```

## Photon Usage

You can use a specific Photon built for this migration (20191103155958-add-bass-and-fun-to-reviews)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191103155958-add-bass-and-fun-to-reviews'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
