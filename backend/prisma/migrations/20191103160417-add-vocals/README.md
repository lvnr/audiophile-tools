# Migration `20191103160417-add-vocals`

This migration has been generated by lvnr at 11/3/2019, 4:04:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."HeadphoneReview" ADD COLUMN "vocals" integer   ,
ADD COLUMN "vocalsUrl" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191103155958-add-bass-and-fun-to-reviews..20191103160417-add-vocals
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://audiophile:LevAr8523*@audiophile-tools.clqkmkirkmxi.us-east-1.rds.amazonaws.com:5432/prisma"
 }
@@ -97,8 +97,10 @@
   imaging           Int?
   imagingUrl        String?
   separation        Int?
   separationUrl     String?
+  vocals            Int?
+  vocalsUrl         String?
   fun               Int?
   funUrl            String?
   bass              Int?
   bassUrl           String?
```

## Photon Usage

You can use a specific Photon built for this migration (20191103160417-add-vocals)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191103160417-add-vocals'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
